# pdf-to-editable-powerpoint v4 (LaMa local inpainting)
# 100% local — no API keys, no cloud, no rate limits.

# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
python-multipart>=0.0.12

# --- Conversion pipeline ---
pdfplumber>=0.10              # PDF page rendering during conversion (uses pypdfium2 internally)
pypdfium2>=4.18               # also used directly in page_render.py for browser thumbnails (Apache 2.0 / BSD-3, no AGPL)
easyocr>=1.7,<2.0             # local OCR with native Korean+English models (PyTorch backend)
simple_lama_inpainting>=0.1   # LaMa wrapper for text-removal inpainting
torch>=2.0                    # required by easyocr + simple_lama_inpainting (CPU build OK)
torchvision>=0.15
opencv-python>=4.8            # mask morphology
numpy>=1.24,<2.0              # simple_lama_inpainting 0.1.x is pinned to numpy <2.0
Pillow>=10.0,<11.0            # simple_lama_inpainting 0.1.x is pinned to pillow <11.0
python-pptx>=1.0,<2.0         # write the .pptx

# --- Models ---
pydantic>=2.0,<3.0

# --- Dev / test ---
pytest>=8.0
httpx>=0.27
